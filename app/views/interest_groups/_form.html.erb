<%= form_for(@interest_group) do |f| %>
  <% if @interest_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@interest_group.errors.count, "error") %> prohibited this interest_group from being saved:</h2>

      <ul>
      <% @interest_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :tag %><br>
    <%= f.text_field :tag %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
